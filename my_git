 Удали из индекса все файлы (не из проекта):
git rm -r --cached -f .
🔸 Это не удалит файлы с диска, только из Git'а.
🔸 --cached означает: удалить из индекса (staging), но оставить на месте.

2. Добавь снова, с учётом нового .gitignore:

git add .
